fn death_message(): void {
  let player_name = game_values::name;
  let message = "died!";
  let emoji = "💀";

  let numbers: dict[number];
  let other: list[list[list[list[number]]]];

  if player is_sneaking() {
    message = "died while sneaking!";
    emoji = "😬";
  }

  if (numbers) {
    player_action::send_message("Hello, World!");
  }

  all_players {
    player_action::send_message(
      emoji, player_name, message, emoji,
      alignment_mode='Centered'
    );
  }
}

event player_event::join {
  player_action::send_message("Basalt death message example.");
}

event player_event::death {
  @global death_message();
}