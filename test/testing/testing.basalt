
fn something(): list[string] {
  return ['abc', 'def'];
}

event player_event::command {
  if game command_equals('@greet') {
    let @line sender = game_values::name;
    let @line players = game_values::plot_player_uuids as list[string];

    for @line uuid in @line players {
      selection {
        select_object::player_name(@line uuid);
        player_action::send_message("Hello from", @line sender);
        select_object::reset();
      }
    }
  }

  if game command_equals('@test') {
    let @line test = {
      'key': {
        'other': 0
      }
    };

    let @line something = @line test['foo']['other'];

    som

    @line test['foo'] = {} as dict[number];

    @line test['foo']['bar'] += 10;
    @line test['goo']['boo'] = 2.3;
    
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();
    control::wait();

    player_action::send_message(@line test['foo']['bar'] as text);

    # for @line key, @line value in @line test {
    #   player_action::send_message(@line key as text, @line test as text);
    # }
  }
}

event player_event::join {
  # repeat forever {
  #   while player is_sneaking() {
  #     player_action::action_bar("sneaker");
  #   }
  #   
  #   control::wait(1);
  # }
}